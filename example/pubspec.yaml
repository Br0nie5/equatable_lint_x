name: equatable_lint_x_example
description: Package with every examples for the equatable_lint_x package
version: 0.1.0

publish_to: none

environment:
  sdk: ^3.9.0

dependencies:
  equatable: ^2.0.8

dev_dependencies:
  equatable_lint_x:
    path: ../
